BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[AdditionalLaborRate] ALTER COLUMN [rate] INT NOT NULL;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
